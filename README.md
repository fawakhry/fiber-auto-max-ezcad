# مطبعجي فايبر ليزر — V5.8.0

برنامج متصفح عربي لتجهيز صور الحفر على ماكينة فايبر 30W.

## مساران منفصلان

### 1) حفر صورة

- رفع صورة شخص كاملة.
- عزل AI احترافي باستخدام MODNet مع MediaPipe كخطة احتياطية.
- Alpha Matting للشعر والحواف وتقليل الهالة البيضاء.
- قص تلقائي قريب من الشخص بدون فريم أبيض زائد.
- تحكم جودة: تنظيف الخلفية، نعومة الحواف، تفاصيل الوجه، وقوة الأسود.
- توزيع إضاءة تلقائي لإزالة الظلال وتخفيف المناطق المحروقة قبل الحفر.
- زر توضيح مجاني باستخدام Swin2SR داخل المتصفح.
- تصدير صورة الشخص PNG على خلفية بيضاء ثابتة.
- محرك Leather Photo يصنع تنقيط Stucki عند المقاس وDPI النهائيين، بدل Threshold الذي كان يحول الملابس إلى كتل سوداء.
- ضغط الظلال وحد أقصى لكثافة النقط يمنع شكل الفحم ويحافظ على طيات الملابس وملامح الوجه.
- زر اختبار جلد يصدر تدريج كثافة 10–70% لمعايرة الطاقة على قصاصة من الخامة نفسها.
- تصدير حفر PNG / SVG / BMP / PDF بخلفية بيضاء.

### 2) حفر عين

- يفتح اختيار صورة جديدة مستقلة.
- اختيار «صورة كاملة» لاكتشاف الوجه وقص العينين والحاجبين تلقائيًا.
- اختيار «عين مقصوصة» لمعالجة القص الجاهز مباشرة.
- لا يعتمد على صورة الشخص في المسار الأول.
- توضيح AI اختياري ×2.
- وضع «محفظة واقعية» بتنقيط Stucki متدرج بدل الكتل السوداء.
- وضع Jarvis فوتوغرافي ووضع خطوط قوية.
- توزيع إضاءة الجلد والحفاظ على الجفن والرموش والقزحية والحواجب.
- اختفاء ناعم للجلد من الأطراف بدون فريم مستطيل.
- ضبط عرض الحفر الحقيقي وDPI قبل صناعة النقط.
- إعادة تحجيم الناتج بعد قص الفراغات حتى يظل عرض العين النهائي مطابقًا للمليمتر المطلوب.
- كثافة خفيفة افتراضيًا للجلد حتى لا تتحول العين لمستطيل أسود.
- كارت إعداد EZCAD يوضح إغلاق Dither وGray وLighten وPixel Power Adjust لأن الملف جاهز.
- قص الفراغات لأقصى حد.
- تصدير PNG على خلفية بيضاء أو SVG.

## إصدار كامل فقط

افتح `index.html`. لا يوجد وضع تجريبي أو علامة مائية أو قفل للتصدير، سواء كان التشغيل محليًا أو من موقع ويب.

## تحديث النسخ المباعة

- زر «فحص التحديث» يقرأ `version.json` المنشور على GitHub Pages.
- عند إصدار نسخة جديدة غيّر رقم `version` والملاحظات داخل `version.json` وارفع الملفات إلى فرع `main`.
- النسخ الموجودة عند العملاء ستعرض التحديث ورابط تحميل أحدث ZIP تلقائيًا.
- ضع رابط تنزيل خاص وآمن داخل `downloadUrl` عند كل إصدار. لا تنشر ZIP النسخة الكاملة في مستودع عام.

## دقة المقاس

- PNG وBMP يحملان DPI المختار داخل الملف.
- PDF يحافظ على العرض والارتفاع الحقيقيين بالمليمتر بدل تكبير التصميم لملء A4.
- البرنامج يرفض المقاسات التي تتجاوز ذاكرة المتصفح بدل خفض DPI بصمت.
- عند تغيير المقاس أو DPI أو إعدادات الجودة بعد التجهيز، يُغلق التصدير حتى إعادة التجهيز.

## تشغيل صورة الجلد في EZCAD

- ابدأ من `Leather Photo` و333 DPI.
- استورد PNG أو BMP، وشغّل `Fixed DPI` بنفس قيمة الملف.
- اجعل `Gray` و`Dither` و`Lighten` و`Pixel Power Adjust` مغلقة لأن التنقيط تم داخل البرنامج بالفعل.
- شغّل `Drill Mode`، وابدأ أول اختبار مع `Bidirectional Scan` مغلقًا.
- لفايبر 30W: اختبر سرعة 2500–3000 mm/s وتردد 50 kHz وطاقة 8% ثم 12% ثم 16%، بتمريرة واحدة.
- استخدم زر «اختبار الجلد» على قصاصة من نفس المحفظة؛ نوع الجلد واللون والطبقة السطحية يغيّرون النتيجة.
- اقرأ `LEATHER_PHOTO_GUIDE_AR.md` للتفسير الكامل والمصادر وخطة المعايرة.

## الإصدار الكامل

- نفس الوظائف الكاملة تعمل عند فتح الملفات محليًا أو من `localhost` أو من موقع ويب.
- جميع أزرار التصدير متاحة بعد تجهيز الصورة.

## التشغيل

يمكن فتح index.html مباشرة. للحصول على أفضل توافق مع موديلات AI يفضل تشغيل المجلد من localhost بالأمر:

    python -m http.server 8080

ثم افتح:

    http://localhost:8080/

## فحص الإصدار

يتطلب Node.js فقط:

    npm test

ولإعادة بناء ملف الطوارئ أحادي الصفحة:

    npm run build:single

أول استخدام يحتاج إنترنت لتحميل موديلات الذكاء الاصطناعي المجانية. بعد التحميل قد يستفيد المتصفح من الكاش. الصور تعالج داخل المتصفح ولا يتم رفعها إلى خادم البرنامج.

## GitHub Pages

ارفع كل الملفات، ومنها `index.html` و`styles.css` و`app.js` و`update-config.js` و`version.json` و`.nojekyll` في جذر المستودع، ثم فعّل Pages من فرع main.
